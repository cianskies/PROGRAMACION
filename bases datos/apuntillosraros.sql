--crear tablas

CREATE TABLE nombre_tabla{
campo(nombre) tipo(VARCHAR2) modificador{-PRIMARY KEY
								UNIQUE
								NOT NULL
								REFERENCES otra_tabla camppo(nombre) ((clave ajena))
campo tipo modificador check (campo2 { IN ()
							           BETWEEN()
									   LIKE										
									   >7}
campo3 tipo modificador default valor,
CONSTRAINT nombre_restriccion CHECK in,betwwen,like etc


}							

alter TABLE tabla
add constraint ......

tipos{ VARCHAR2(longitud) caractereres
		DATE fecha
		number(num cifras, (num de cuantas ellas son decimales))
		
}
